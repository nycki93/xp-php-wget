# php static site generator

a proof of concept that you can use php's built-in dev server to convert php "templates" into a static site by downloading it with wget.

## usage

requires: php, wget, make

make site
